// Placeholder route required by Expo Router for the Pomodoro tab entry.
// The tab's `tabPress` listener in _layout.tsx intercepts the press and opens
// the PomodoroTimer modal instead of navigating here, so this component
// should never actually render.
export default function PomodoroRoute() {
    return null;
}
