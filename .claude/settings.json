{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '^[[:space:]]*git[[:space:]]+(commit|push)' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"Reminder from .claude/settings.json: run the /precommit skill before this git commit/push if you have not already run it for these changes (see CLAUDE.md Git Flow rule 6).\"}}' || true"
          }
        ]
      }
    ]
  }
}
